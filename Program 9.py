#Program that takes a string and returns a number of length in the string
# Getting a input from the user
Name_Of_String_to_find_length= input('Enter any string to find the number of length in the string:')
#Finding the length of the string
length = len(Name_Of_String_to_find_length)
#Printing the length of the string
print(f'Number of words in the string is {length}')